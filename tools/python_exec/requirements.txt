# Scientific stack available to executed code.
numpy>=1.26
pandas>=2.1
# HTTP + HTML parsing so executed code can fetch pages and crypto-price APIs and
# parse results without a (sandbox-blocked) runtime pip install.
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
# Headless rendering fallback for JavaScript-heavy pages (e.g. SPA profiles whose
# posts load client-side). Browser binaries are installed in the Dockerfile.
playwright>=1.49
