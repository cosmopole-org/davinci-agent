numpy>=1.26
scikit-learn>=1.3
requests>=2.31
# Optional: enable local semantic embeddings by installing sentence-transformers
# (set VECTOR_BACKEND=sentence_transformers). Heavy (pulls torch); off by default.
# sentence-transformers>=2.6
